# Core
numpy>=2.2
tqdm>=4.67
PyYAML>=6.0
python-dotenv>=1.2
scipy>=1.15

# PyTorch
torch>=2.6
torchvision>=0.21

# HuggingFace
transformers>=4.57
accelerate>=1.12
bitsandbytes>=0.48
datasets>=4.3
huggingface-hub>=0.36
sentence-transformers>=5.6

# Vision
pillow>=12.0

# Time series
tslearn>=0.8
# momentfm — install via: pip install git+https://github.com/moment-timeseries-foundation-model/moment.git
# (PyPI package name is momentfm; momentresearch is a fallback alias if ever published)

# Visualization (used by VisionTSRetriever to render TS line plots)
matplotlib>=3.9

# ML metrics (optional — eval falls back gracefully if missing)
scikit-learn>=1.7

# Logging
wandb>=0.25

# Inference (optional — only needed for vLLM-backed models)
# vllm>=0.8

# Cloud API models (optional — install whichever backends you use)
# openai>=2.14
# anthropic
# google-generativeai>=0.8
